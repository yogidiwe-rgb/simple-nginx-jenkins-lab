apiVersion: v1
kind: Service
metadata:
  name: simple-nginx-app
  labels:
    app: simple-nginx-app
spec:
  type: NodePort
  selector:
    app: simple-nginx-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
